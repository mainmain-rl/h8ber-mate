# pytest configuration file

[pytest]
testpaths = backend/tests
python_files = test_*.py
addopts = --maxfail=3 --disable-warnings

# Enable asyncio mode for testing asynchronous code
asyncio_mode = auto

# Add path to backend src directory
import_paths =
    backend/src

# Mark test files as Python module
python_modules = pytest.ini
